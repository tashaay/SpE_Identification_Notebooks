name: mynewenv
channels:
  - defaults
dependencies:
  - jupyterlab
  - netcdf4
  - pandas
  - matplotlib
  - geos
  - cartopy
  - xarray
  - seaborn
  - dask
prefix: /localhome/sestay/miniconda3/envs/myenv
